@model List<Category>

    @foreach (var cat in Model)
    {
        <li class="">
            <a href="#"
           class="hover:text-[#4E97FD] duration-200 px-5 py-2 block">@cat.category_name</a>
        </li>
    }